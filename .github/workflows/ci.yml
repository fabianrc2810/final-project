name: CI

on:
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
    - name: Login to DockerHub Registry
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login docker.pkg.github.com -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
    
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install

    - name: Run linter
      run: npx eslint .

    - name: Run unit tests
      run: npm test

    - name: Build Docker image
      run: |
        COMMIT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
        DATE=$(date +'%Y%m%d')
        IMAGE_TAG="$DATE-$COMMIT_SHA"
        docker build -t fabianrc2810/final-project:$IMAGE_TAG .

    - name: Push Docker image to Docker Hub
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        COMMIT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
        DATE=$(date +'%Y%m%d')
        IMAGE_TAG="$DATE-$COMMIT_SHA"
        echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
        docker tag fabianrc2810/final-project:$IMAGE_TAG fabianrc2810/final-project:$IMAGE_TAG
        docker push fabianrc2810/final-project:$IMAGE_TAG
